---
phase: 03-live-full-rfq-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../trustrfq-maker-server/package.json
  - ../trustrfq-maker-server/tsconfig.json
  - ../trustrfq-maker-server/vercel.json
  - ../trustrfq-maker-server/.gitignore
  - ../trustrfq-maker-server/README.md
  - ../trustrfq-maker-server/src/wire.ts
  - ../trustrfq-maker-server/src/order.ts
  - ../trustrfq-maker-server/src/config.ts
  - ../trustrfq-maker-server/src/signing.ts
  - ../trustrfq-maker-server/api/rpc.ts
  - ../trustrfq-maker-server/scripts/bootstrap.mjs
  - ../trustrfq-maker-server/scripts/live-proof.mjs
  - ../trustrfq-maker-server/src/order.test.ts
  - ../trustrfq-maker-server/src/signing.test.ts
  - ../trustrfq-maker-server/fixtures/rfq-order-vectors.json
autonomous: false
requirements: [E2E-02]

user_setup:
  - service: vercel
    why: "D-05 — the reference maker server needs its own Vercel project so it gets a real permanent https origin. Claude can run the CLI but cannot create the account or approve the first project link."
    env_vars:
      - name: VERCEL_TOKEN
        source: "Vercel Dashboard -> Account Settings -> Tokens -> Create Token (scope: the account that will own the maker project). Export it in the shell that runs the deploy, or run `vercel login` once instead."
    dashboard_config:
      - task: "Confirm the account can create a SECOND Vercel project (the desk already occupies one)"
        location: "Vercel Dashboard -> Overview -> Add New -> Project"

estimate:
  tokens: 74000
  raw_tokens: 74000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "A POST of a Stellar RFQ v1 `getMakerSideOrder` request to the deployed maker origin returns a JSON-RPC 2.0 result whose `order.maker` is the persistent maker address and whose `authEntry` decodes to a SorobanAuthorizationEntry with an Address credential for that same address"
    - "An OPTIONS preflight to the deployed maker origin returns 204 with access-control-allow-origin, access-control-allow-methods and access-control-allow-headers set"
    - "A request naming a token address outside the maker's configured allow-list is answered with JSON-RPC error code -33601 and HTTP 200, never a 4xx/5xx and never a signed quote"
    - "The maker address is registered on the live rfq_registry: get_maker returns its https url, both the native SAC and the demo USDC SAC in its token list, and a non-zero staked amount"
    - "scripts/live-proof.mjs settles a quote fetched live from the deployed origin on Testnet and prints a transaction hash"
    - "The sibling repo's orderScValBase64 reproduces every vector in fixtures/rfq-order-vectors.json byte for byte, proving no encoding drift from this repo's src/core/rfq/order.ts"
    - statement: "Re-running scripts/bootstrap.mjs after a quarterly Testnet reset restores the maker's funding, trustline, inventory and registry entry in one command"
      verification: backstop
  artifacts:
    - path: "../trustrfq-maker-server/api/rpc.ts"
      provides: "The single Vercel Node serverless function: CORS + OPTIONS + JSON-RPC 2.0 dispatch for getMakerSideOrder"
      contains: "getMakerSideOrder"
    - path: "../trustrfq-maker-server/src/signing.ts"
      provides: "priceQuote (D-04 fixed rate + spread) and signQuote (RECORDING-mode simulate + authorizeEntry)"
      exports: ["priceQuote", "signQuote"]
    - path: "../trustrfq-maker-server/src/config.ts"
      provides: "Typed env reader: MAKER_SECRET (throws when unset), contract ids, rate/spread, the token allow-list"
      exports: ["makerKeypair"]
    - path: "../trustrfq-maker-server/src/order.ts"
      provides: "Verbatim-copied atomic/ScVal encoding — the signature boundary shared with this repo"
      exports: ["toAtomic", "atomicToDecimal", "orderToScVal", "orderScValBase64"]
    - path: "../trustrfq-maker-server/src/wire.ts"
      provides: "Verbatim-copied Stellar RFQ v1 wire types and RFQ_ERROR code map"
      exports: ["RFQ_ERROR"]
    - path: "../trustrfq-maker-server/scripts/bootstrap.mjs"
      provides: "D-12 one-command re-bootstrap: persistent-key guard, fund, trustline, USDC inventory, set_url, add_tokens"
    - path: "../trustrfq-maker-server/scripts/live-proof.mjs"
      provides: "Live end-to-end proof: fetch a quote from the deployed origin and settle it on Testnet with balance-delta assertions"
    - path: "../trustrfq-maker-server/fixtures/rfq-order-vectors.json"
      provides: "Copy of this repo's golden vectors — the anti-drift tripwire between the two repos"
  key_links:
    - from: "../trustrfq-maker-server/api/rpc.ts"
      to: "../trustrfq-maker-server/src/signing.ts"
      via: "handler calls priceQuote then signQuote before writing the JSON-RPC result"
      pattern: "signQuote"
    - from: "../trustrfq-maker-server/src/signing.ts"
      to: "../trustrfq-maker-server/src/order.ts"
      via: "signQuote encodes the order with orderToScVal before the RECORDING-mode simulate"
      pattern: "orderToScVal"
    - from: "../trustrfq-maker-server/scripts/bootstrap.mjs"
      to: "rfq_registry (CBA43RFMQBPBHVQENUZK5OMTE2MRC3BLHFKA7FWXUHNIQ2GSORUNIU5G)"
      via: "invokeRegistry set_url + add_tokens signed by the persistent maker keypair"
      pattern: "set_url|add_tokens"
  prohibitions:
    - statement: "MUST NOT present any stubbed component (the local stub maker, a mocked wallet as sole evidence, a hardcoded maker URL, or a patched discovery response) as satisfying the E2E-02 live-loop claim"
      status: unverified
    - statement: "MUST NOT report only a hand-picked successful attempt: every attempt made during a recorded live run, including dropped or timed-out fan-outs, must appear in the run record"
      status: unverified
    - statement: "MUST NOT cite desk interface metrics (click counts, wallet-prompt counts, visitor numbers) as evidence for the milestone claim (POS-D1)"
      status: unverified
---

<objective>
Stand up the reference maker quote server in its own sibling repository, deploy it to a real
permanent https origin, register it on the live `rfq_registry` with a real stake, and prove the
whole new component end-to-end by settling one of its own signed quotes on Testnet.

Purpose: E2E-02's counterparty does not exist yet. Everything else in this phase (CSP wiring, the
live driver, the recorded evidence) is blocked on a real maker answering at a real origin. This
plan is the tracer slice: the thinnest path through every layer the sibling repo owns — HTTP
transport, pricing, auth-entry signing, registry registration, on-chain settlement — wired for
keeps, not as a prototype.

Output: the `trustrfq-maker-server` repository, a live https endpoint, a permanently registered
maker on the Testnet registry, and one Testnet transaction hash proving a quote from that endpoint
settles.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-live-full-rfq-loop/03-CONTEXT.md
@.planning/phases/03-live-full-rfq-loop/03-RESEARCH.md
@.planning/phases/03-live-full-rfq-loop/03-PATTERNS.md

@tools/e2e/stub-maker.mjs
@tools/rfq-registry-live.mjs
@src/core/rfq/wire.ts
@src/core/rfq/order.ts
</context>

## Artifacts this phase produces

New symbols and paths created by Phase 3 (none of these exist before this phase; treat every name
below as net-new, not as drift from an existing API):

**Sibling repository `trustrfq-maker-server` (created by this plan, at `../trustrfq-maker-server`
relative to this repo):**
- Files: `package.json`, `tsconfig.json`, `vercel.json`, `.gitignore`, `README.md`,
  `api/rpc.ts`, `src/wire.ts`, `src/order.ts`, `src/config.ts`, `src/signing.ts`,
  `src/order.test.ts`, `src/signing.test.ts`, `scripts/bootstrap.mjs`, `scripts/live-proof.mjs`,
  `fixtures/rfq-order-vectors.json`, `maker-keys.json` (gitignored, never committed)
- Exported functions: `priceQuote`, `signQuote` (`src/signing.ts`); `toAtomic`, `atomicToDecimal`,
  `orderToScVal`, `orderScValBase64` (`src/order.ts`); `makerKeypair`, `RATE_MID`, `SPREAD_BPS`,
  `ALLOWED_TOKENS`, `RFQ_SWAP_CONTRACT_ID`, `RFQ_REGISTRY_ID`, `PASSPHRASE`, `RPC_URL`
  (`src/config.ts`); default `handler` (`api/rpc.ts`)
- Constants: `CORS_HEADERS`, `QUOTE_TTL_SEC`, `SIGNATURE_LEDGER_WINDOW` (`api/rpc.ts` /
  `src/signing.ts`)
- Env vars: `MAKER_SECRET`, `USDC_ISSUER`, `USDC_ISSUER_SECRET`, `MID_RATE_USDC_PER_XLM`,
  `SPREAD_BPS`, `MIN_TAKER_AMOUNT`, `MAX_TAKER_AMOUNT`, `RFQ_SWAP_CONTRACT_ID`,
  `RFQ_REGISTRY_ID`, `RPC_URL`, `NETWORK_PASSPHRASE`, `MAKER_PUBLIC_URL`
- CLI flags: `--fresh-maker` (`scripts/bootstrap.mjs`)
- npm scripts: `test`, `bootstrap`, `live-proof`, `deploy` (sibling repo `package.json`)

**This repository (later plans in this phase):**
- `tools/e2e/rfq-driver.mjs` live mode: env vars `REAL_MAKER_ADDRESS`, `LIVE_DIRECTIONS`,
  `LIVE_RECORD`; functions `resolveRealMaker`, `readRegisteredMaker`, `snapshotBalances`,
  `runLiveDirection`; report fields `mode`, `makerAddress`, `makerUrlOnChain`, `directions`,
  `cspViolations`, `attempts`
- npm script `e2e:rfq:live` (`package.json`)
- New files `docs/evidence/live-rfq-run.json`, `docs/evidence/live-rfq-run.md`

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate — Vercel tooling before first install</name>
  <what-built>Nothing yet. This gate runs BEFORE any package-manager install in the new sibling
  repo. 03-RESEARCH.md's Package Legitimacy Audit flagged `vercel` (CLI) and `@vercel/node` as
  [SUS] under the "too-new" heuristic — both are official `github.com/vercel/vercel` packages with
  multi-million weekly downloads and no postinstall script, but neither has been used in this
  project before, so protocol requires a human to confirm them before installation.
  `@stellar/stellar-sdk` is exempt (already running in production in this repo at `^16.0.1`).
  `typescript` verdict is OK.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vercel — confirm the repository field points at
       github.com/vercel/vercel, the publisher is the `vercel` org, weekly downloads are in the
       millions, and the package has no install/postinstall script listed.
    2. Open https://www.npmjs.com/package/@vercel/node — confirm the same three properties.
    3. Confirm the version to pin: `npm view vercel version` and `npm view @vercel/node version`.
    4. Decide whether `@vercel/node` is wanted at all. It is needed ONLY for the `VercelRequest`/
       `VercelResponse` TypeScript types in `api/rpc.ts`; a plain handler typed against Node's own
       `IncomingMessage`/`ServerResponse` needs no such dependency.
  </how-to-verify>
  <acceptance_criteria>
    - Human has visited both npm pages and confirmed org, download volume, and absence of a
      postinstall script
    - Human states which of the two packages is approved for install, and the version to pin
    - This checkpoint is never auto-approved, regardless of `workflow.auto_advance`
  </acceptance_criteria>
  <resume-signal>Type "approved: vercel@&lt;version&gt;[, @vercel/node@&lt;version&gt;]" or describe the concern</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: The maker server exists, is deployed at a real https origin, and returns a genuinely signed quote</name>
  <precondition>The Vercel account named in `user_setup` can create a second project, and either `VERCEL_TOKEN` is exported in the shell or `vercel whoami` succeeds.</precondition>
  <reversibility rating="costly">The maker's persistent Stellar identity is what makes the registry
  entry ejectable; if `MAKER_SECRET` is ever lost the entry can never be ejected and the stake is
  permanently locked on the live registry.</reversibility>
  <files>../trustrfq-maker-server/package.json, ../trustrfq-maker-server/tsconfig.json, ../trustrfq-maker-server/vercel.json, ../trustrfq-maker-server/.gitignore, ../trustrfq-maker-server/README.md, ../trustrfq-maker-server/src/wire.ts, ../trustrfq-maker-server/src/order.ts, ../trustrfq-maker-server/src/config.ts, ../trustrfq-maker-server/src/signing.ts, ../trustrfq-maker-server/api/rpc.ts, ../trustrfq-maker-server/scripts/bootstrap.mjs</files>
  <read_first>
    - tools/e2e/stub-maker.mjs (the seed — read the whole file; lines 60-120 for the env/config
      reads, 208-260 for `toAtomic`/`atomicToDecimal`/`orderScVal`/`isAddressCredential`/
      `toContractOrder`, 279-326 for `signQuote`, 338-375 for `handleGetMakerSideOrder`, 377-556
      for the CORS constant and JSON-RPC dispatch, 559-593 for `setup`'s fund/trustline/inventory/
      register sequence)
    - src/core/rfq/wire.ts (the file to copy verbatim into the sibling repo)
    - src/core/rfq/order.ts (the file to copy into the sibling repo, minus `sacIdFor`/`tokenForSac`)
    - tools/rfq-registry-live.mjs (lines 178-193 `invokeRegistry`: simulate -> assembleTransaction
      -> sign -> sendTransaction -> pollTransaction)
    - src/data/rfqNetwork.ts (lines 60-95: the exact request body and drop semantics the maker must
      satisfy — HTTP 200, a `result` carrying `order` and `authEntry`, within a 3s client timeout)
    - src/core/tokens.ts (the curated allow-list and the demo USDC issuer the maker must quote)
    - .gitignore (this repo's gitignored-secret convention: `demo-keys.json`, `e2e-keys.json`,
      `.vercel`, `node_modules`, `dist/`)
    - .planning/phases/03-live-full-rfq-loop/03-PATTERNS.md (the per-file analog assignments)
  </read_first>
  <action>
    Create the sibling repository at `../trustrfq-maker-server` (a git repo of its own, initialised
    with `git init`; it is NOT a subdirectory of this repo and none of its code may ever be copied
    back here — D-02). Name it `trustrfq-maker-server` per 03-RESEARCH.md's Open Questions section.
    Per D-01 the whole server is a refactor of `tools/e2e/stub-maker.mjs`'s already-working signing
    and wire logic rather than a from-scratch build: keep everything that signs and everything that
    speaks the wire, drop every test-only failure knob, and add the persistent identity and server
    config the stub deliberately lacks.

    Scaffold: `package.json` with `"type": "module"`, `"engines": { "node": ">=20.19" }`,
    dependency `@stellar/stellar-sdk@^16.0.1` (pin the same major this repo runs — see 03-RESEARCH.md
    assumption A3; do NOT take 17.x), devDependencies `typescript` plus whichever Vercel packages
    Task 1 approved, and scripts `test`, `bootstrap` (`node scripts/bootstrap.mjs`), `live-proof`
    (`node scripts/live-proof.mjs`) and `deploy` (`vercel deploy --prod --yes`). `tsconfig.json`
    with `"module": "ESNext"`, `"moduleResolution": "Bundler"`, `"target": "ES2022"`, `"strict": true`.
    `.gitignore` carrying `node_modules`, `.vercel`, `dist/`, `.env*`, `maker-keys.json`.
    `README.md` stating what the server is, the D-03 v1 surface, the env vars, and the two commands
    (`npm run bootstrap`, `npm run deploy`).

    `src/wire.ts`: copy `src/core/rfq/wire.ts` from THIS repo verbatim (the `RfqOrder`,
    `GetMakerSideOrderParams`, `MakerSideOrderResult` interfaces and the `RFQ_ERROR` map). Add one
    header comment recording the origin repo path and the origin commit hash (`git rev-parse HEAD`
    in this repo at copy time) — 03-RESEARCH.md Pitfall 4's traceability mitigation. Do not rename
    or restructure a single field: `src/data/rfqNetwork.ts` in this repo decodes exactly this shape
    and a drift fails silently as a taker-side rejection, not a compile error.

    `src/order.ts`: copy `toAtomic`, `orderToScVal` and `orderScValBase64` from this repo's
    `src/core/rfq/order.ts` verbatim, including the `.sort()` line and its comment about
    alphabetical Symbol keys. Do NOT copy `sacIdFor`/`tokenForSac` (they depend on
    `src/core/canonical.ts` and `src/core/tokens.ts`, neither of which belongs in the maker repo).
    Add `atomicToDecimal` ported from `tools/e2e/stub-maker.mjs` lines 214-221 (BigInt only, no
    floating point). Carry the same origin-commit header comment.

    `src/config.ts`: the one typed env reader, modelled on this repo's `src/config.ts` convention
    (one module reads the environment, everything else imports from it). Export `makerKeypair`
    derived from `Keypair.fromSecret(process.env.MAKER_SECRET)` and THROW a clear error when
    `MAKER_SECRET` is unset — never fall back to `Keypair.random()` the way
    `tools/e2e/stub-maker.mjs` line 101 does, because the maker's registry identity must survive
    every redeploy and cold start (D-10, 03-RESEARCH.md Pattern 2). Also export `RPC_URL`
    (default `https://soroban-testnet.stellar.org`), `PASSPHRASE` (default
    `Test SDF Network ; September 2015`), `RFQ_SWAP_CONTRACT_ID` (default
    `CCNP7626WIJVWVTBPLPG6QM77TY6JBU42D4PYONUFTDEPIIW6ZFJQIDT`), `RFQ_REGISTRY_ID` (default
    `CBA43RFMQBPBHVQENUZK5OMTE2MRC3BLHFKA7FWXUHNIQ2GSORUNIU5G`), `RATE_MID` from
    `MID_RATE_USDC_PER_XLM` (default the decimal string `2.5`), `SPREAD_BPS` (default `0`),
    `MIN_TAKER_AMOUNT`/`MAX_TAKER_AMOUNT` decimal-string bounds, and `ALLOWED_TOKENS`: a Map from
    SAC contract id to a token descriptor, built at module load from `Asset.native().contractId(PASSPHRASE)`
    and `new Asset('USDC', process.env.USDC_ISSUER ?? 'GBJH2XCGKRMFKCBYPFJHHGISZGLOYZ3TM3IMFQPQSK7NT2L7JUARLC26').contractId(PASSPHRASE)`.
    That issuer default is THIS repo's temporary demo issuer from `src/core/tokens.ts` — the maker
    must sell the same asset the desk knows. Also export `MAKER_PUBLIC_URL` (the registered
    endpoint URL, e.g. the deployed origin with `/api/rpc` appended).

    `src/signing.ts`: two exported functions.
    `priceQuote(params)` implements D-04's fixed configured rate plus optional spread, entirely in
    BigInt atomic units with no floating point. Reject any `makerToken` or `takerToken` not present
    in `ALLOWED_TOKENS` by throwing a tagged error carrying `RFQ_ERROR.PAIR_NOT_TRADED`; reject a
    `takerAmount` that is not a well-formed decimal string, or that falls outside
    `MIN_TAKER_AMOUNT`/`MAX_TAKER_AMOUNT`, with `RFQ_ERROR.INVALID_PARAMS`,
    `RFQ_ERROR.AMOUNT_TOO_LOW` or `RFQ_ERROR.AMOUNT_TOO_HIGH` respectively. Compute `makerAmount`
    for both directions from a single mid rate expressed in atomic units: when the taker sells the
    native SAC the maker pays USDC at `takerAtomic * midAtomic / 1e7`, when the taker sells USDC
    the maker pays XLM at `takerAtomic * 1e7 / midAtomic`, and in both directions the maker's
    output is reduced by `SPREAD_BPS` (floor division, so the maker never over-pays by rounding).
    Build the `RfqOrder` with `maker` = the maker public key, `taker` = `params.takerWallet`,
    `expiry` = now plus `QUOTE_TTL_SEC` (90) seconds, `feeBps` read live from `rfq_swap`'s
    `get_config` (mirror `tools/e2e/stub-maker.mjs`'s `readFeeBps`, lines 195-206, cached per warm
    instance), and `orderId` = `Date.now() * 1000 + a random integer below 1000`. That orderId is
    load-bearing twice over: it is the probe transaction's source sequence, and
    `tools/e2e/stub-maker.mjs`'s header comment at lines 287-304 documents a live
    `Error(Auth, ExistingValue)` nonce collision caused by reusing a constant there; a
    millisecond-plus-random value also stays unique across concurrent serverless instances.
    `signQuote(order)` is the port of `tools/e2e/stub-maker.mjs` lines 279-326 with the `expired`
    knob and the `CAPTURE_AUTH_TREE` branch removed (D-03 excludes every test-only knob): build a
    `swap` call on `RFQ_SWAP_CONTRACT_ID` with `orderToScVal`, source it from
    `new Account(order.taker, String(order.orderId))`, `simulateTransaction` in RECORDING mode,
    find the entry whose credential is an Address credential for the maker, and
    `authorizeEntry(entry, makerKeypair, latestLedger + 180, PASSPHRASE)`. Cache the latest ledger
    sequence in module scope with a staleness window of 5000ms so a warm invocation reuses it and
    only a cold one pays a `getLatestLedger` round trip — 03-PATTERNS.md records this as the
    dominant latency saving against the taker's 3s fetch timeout, and a serverless function cannot
    keep `setInterval` alive across cold starts the way the stub maker's long-lived process does.
    Return `{ order, authEntry, signatureExpirationLedger, network, swapContract }` — exactly the
    `MakerSideOrderResult` shape `src/data/rfqNetwork.ts` decodes.

    `api/rpc.ts`: the single Vercel Node serverless function, default-exported `handler(req, res)`.
    Port `tools/e2e/stub-maker.mjs`'s `CORS_HEADERS` constant (lines 385-390) verbatim —
    `access-control-allow-origin: *`, `access-control-allow-methods: POST, OPTIONS`,
    `access-control-allow-headers: content-type` — and add `access-control-max-age: 86400`. The
    wildcard origin is deliberate and is the same reasoning the stub maker's own header comment
    records: a maker server cannot enumerate every taker origin in advance, and the maker's
    signature over the economic terms is the real trust boundary, not CORS. Vercel adds no CORS
    headers on its own, so an `OPTIONS` branch returning 204 with those headers must be the first
    thing the handler does; without it the browser's preflight fails and the maker looks entirely
    unreachable (03-RESEARCH.md Pitfall 2). Then: reject non-POST with 405 plus the CORS headers;
    parse the body defensively and answer a malformed body with JSON-RPC `-32700`; answer a
    `method` other than `getMakerSideOrder` with `-32601` — the ONLY method this server implements
    per D-03, with no `getPricing`, no `getTakerSideOrder`, no WSS, no health endpoint. On the
    happy path call `priceQuote` then `signQuote` and write `{ jsonrpc, id, result }`. Every
    failure path answers HTTP 200 with a JSON-RPC `error` object carrying the tagged
    `RFQ_ERROR` code the thrown error names, falling back to `RFQ_ERROR.CANNOT_PROVIDE_ORDER`;
    when the RECORDING simulation fails for lack of a destination trustline answer
    `RFQ_ERROR.TAKER_TRUSTLINE_MISSING`, and when it fails for lack of maker balance answer
    `RFQ_ERROR.MAKER_INVENTORY_UNAVAILABLE`. Never return a non-200 HTTP status for a
    business-logic failure: this repo's `getMakerSideOrder` treats any non-2xx as an unreachable
    maker and drops it without surfacing the reason. Never log the secret key or the signed entry's
    signature bytes.

    `scripts/bootstrap.mjs`: the D-12 one-command re-bootstrap, patterned on
    `tools/e2e/stub-maker.mjs`'s `setup()` and `tools/rfq-registry-live.mjs`'s `invokeRegistry`.
    Key handling first, and fail loudly: read the maker secret from `MAKER_SECRET`, else from a
    gitignored `maker-keys.json` in the repo root; if neither exists AND `--fresh-maker` was not
    passed, exit non-zero with a message naming both sources. Only `--fresh-maker` may generate a
    new keypair, and it must write it to `maker-keys.json` before doing anything on-chain. This
    guard is the mitigation for the orphaned-registry-entry failure mode
    `tools/e2e/rfq-driver.mjs` lines 874-896 documents: a maker key that is lost after
    registration leaves a permanently staked entry no later process can ever sign an `eject` for.
    Then, idempotently: Friendbot-fund the maker (treat an already-exists 400 as success, mirroring
    `friendbot()` in the stub maker), open the USDC trustline if absent, have the issuer
    (`USDC_ISSUER_SECRET` from env or `maker-keys.json`) pay the maker its USDC inventory, then
    `set_url` with `MAKER_PUBLIC_URL` and `add_tokens` with both SAC addresses. A second `set_url`
    is a free in-place update, so call both unconditionally rather than branching on "already
    registered". Print the maker address, the registered URL, both SAC ids, and the `get_maker`
    read-back at the end.

    Deploy (D-05 — the maker gets its OWN Vercel project and a real permanent https origin; a
    localhost URL or a tunnel was rejected because a churning or unreachable origin is not evidence
    anyone can re-check): run `vercel link --yes` then `vercel env add MAKER_SECRET production`
    (marking it Sensitive) and the non-secret env vars, then `vercel deploy --prod --yes`. Record
    the resulting production origin. Set `MAKER_PUBLIC_URL` to that origin with `/api/rpc` appended, redeploy so
    the function sees it, then run `npm run bootstrap`. The maker secret must exist ONLY in the
    Vercel Sensitive env var and in the gitignored local key file — never in a committed file,
    never in a `NEXT_PUBLIC_`-style prefixed variable, never in a log line.
  </action>
  <verify>
    <automated>cd ../trustrfq-maker-server &amp;&amp; npx tsc --noEmit &amp;&amp; ORIGIN="$(node -e "process.stdout.write(new URL(process.env.MAKER_PUBLIC_URL).origin)")" &amp;&amp; curl -sS -o /dev/null -w '%{http_code}\n' -X OPTIONS -H 'origin: https://example.invalid' -H 'access-control-request-method: POST' "$MAKER_PUBLIC_URL" | grep -qx 204 &amp;&amp; curl -sS -X POST -H 'content-type: application/json' -d "{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"getMakerSideOrder\",\"params\":{\"network\":\"Test SDF Network ; September 2015\",\"swapContract\":\"$RFQ_SWAP_CONTRACT_ID\",\"makerToken\":\"$USDC_SAC\",\"takerToken\":\"$NATIVE_SAC\",\"takerAmount\":\"1.0000000\",\"takerWallet\":\"$PROBE_TAKER\",\"minExpiry\":$(( $(date +%s) + 30 ))}" "$MAKER_PUBLIC_URL" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);if(!r.result||!r.result.authEntry||!r.result.order)throw new Error('no signed quote: '+s);process.stdout.write('signed quote ok maker='+r.result.order.maker+'\n')})"</automated>
  </verify>
  <acceptance_criteria>
    - `cd ../trustrfq-maker-server &amp;&amp; npx tsc --noEmit` exits 0
    - An OPTIONS request to `MAKER_PUBLIC_URL` returns HTTP 204 and carries
      `access-control-allow-origin`, `access-control-allow-methods` and `access-control-allow-headers`
    - A well-formed `getMakerSideOrder` POST returns HTTP 200 with a `result` containing `order`,
      `authEntry`, `signatureExpirationLedger`, `network` and `swapContract`
    - `result.order.maker` equals the maker's public key and `result.order.feeBps` equals the value
      `rfq_swap.get_config` currently returns
    - `xdr.SorobanAuthorizationEntry.fromXDR(result.authEntry, 'base64')` decodes and its credential
      is an Address credential for the maker's public key
    - A POST naming a token address outside `ALLOWED_TOKENS` returns HTTP 200 with
      `error.code === -33601`
    - A POST with `method: "getPricing"` returns HTTP 200 with `error.code === -32601`
    - `grep -rn "Keypair.random" ../trustrfq-maker-server/src ../trustrfq-maker-server/api` returns
      no matches
    - `cd ../trustrfq-maker-server &amp;&amp; git status --porcelain --ignored | grep maker-keys.json`
      shows the key file as ignored, and `git ls-files` does not list it
    - `node scripts/bootstrap.mjs` prints a `get_maker` read-back whose `url` equals
      `MAKER_PUBLIC_URL`, whose token list contains both SAC ids, and whose `staked` is non-zero
  </acceptance_criteria>
  <done>A real https origin answers Stellar RFQ v1 `getMakerSideOrder` with a genuinely signed auth
  entry from a persistent maker identity, and that maker is registered on the live Testnet registry
  with a real stake.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the quote settles, and pin the two repos' encoding together</name>
  <precondition>`MAKER_PUBLIC_URL` resolves to the deployed maker origin and `node scripts/bootstrap.mjs` has already registered the maker (Task 2's `<done>`).</precondition>
  <files>../trustrfq-maker-server/scripts/live-proof.mjs, ../trustrfq-maker-server/src/order.test.ts, ../trustrfq-maker-server/src/signing.test.ts, ../trustrfq-maker-server/fixtures/rfq-order-vectors.json, ../trustrfq-maker-server/package.json</files>
  <read_first>
    - tools/rfq-live-swap.mjs (the whole file — the proven live settlement pattern: build the swap
      tx with the taker as source, attach the maker's detached entry, enforcing-mode simulate,
      assemble, sign, send, poll, then assert balance deltas)
    - src/core/rfq/settle.ts (the taker-side settlement path this proof must mirror, so a pass here
      predicts a pass in the browser)
    - contracts/rfq_swap/src/lib.rs (lines 200-280: the argument binding, the `fee_bps` equality
      check, and the three transfers — taker_token taker->maker, maker_token maker->taker,
      maker_token maker->fee_collector)
    - fixtures/rfq-order-vectors.json (the golden vectors to copy)
    - src/core/rfq/order.test.ts (the assertion style to mirror in the sibling repo)
    - ../trustrfq-maker-server/src/signing.ts (as written in Task 2)
  </read_first>
  <action>
    Copy `fixtures/rfq-order-vectors.json` from this repo into the sibling repo at
    `fixtures/rfq-order-vectors.json` byte for byte. Add `src/order.test.ts` asserting that the
    sibling repo's `orderScValBase64` reproduces every vector's `scValBase64` exactly, plus the
    tamper case (one atomic unit of drift in `makerAmount` changes the encoding). This is the
    anti-drift tripwire 03-RESEARCH.md Pitfall 4 demands: two repos maintaining structurally
    identical signing logic with nothing but discipline holding them together, where a silent
    divergence surfaces downstream as a taker-side `tree_mismatch` that looks exactly like a
    malicious maker.

    Add `src/signing.test.ts` covering `priceQuote` only (pure, no network): a table of
    (direction, takerAmount, midRate, spreadBps) cases asserting the exact decimal-string
    `makerAmount`, both directions; a case asserting an unknown token address throws with the
    `-33601` tag; a case asserting a non-decimal `takerAmount` throws with `-33604`; and cases
    asserting the `MIN_TAKER_AMOUNT`/`MAX_TAKER_AMOUNT` bounds throw `-33602`/`-33603`.
    Pick the runner that needs no extra dependency beyond what Task 1 approved: `node --test` with
    Node's built-in `assert`, wired as the `test` script in `package.json`. Amounts are BigInt
    throughout; a test that compares floats is not testing the code that ships.

    Add `scripts/live-proof.mjs`: fetch one real quote from `MAKER_PUBLIC_URL` over HTTP for a
    freshly Friendbot-funded throwaway taker (with its USDC trustline opened first so the maker's
    own RECORDING simulation can succeed — `tools/e2e/rfq-driver.mjs`'s header comment at lines
    66-80 records that a maker cannot even produce a quote for a trustline-less taker), snapshot the
    maker's, the taker's and the fee collector's balances, settle the quote exactly the way
    `src/core/rfq/settle.ts` does (taker as transaction source, the maker's `authEntry` attached to
    the invokeHostFunction operation, enforcing-mode simulate, `assembleTransaction`, sign with the
    taker keypair, send, poll), then snapshot balances again and assert every delta. Read
    `fee_collector` and `fee_bps` from `rfq_swap.get_config`. The five assertions, all in stroops
    and all exact: taker's takerToken falls by `takerAmount` (plus the transaction's own
    `fee_charged` when the taker token is native XLM — read it from the Horizon transaction record,
    do not approximate); taker's makerToken rises by `makerAmount`; maker's takerToken rises by
    `takerAmount`; maker's makerToken falls by `makerAmount + fee`; the fee collector's makerToken
    rises by `fee`, where `fee = makerAmountAtomic * feeBps / 10000` floored, matching
    `mul_bps` in `contracts/rfq_swap/src/lib.rs`. Print the transaction hash, the quoted amounts,
    and every measured delta alongside its expected value. Exit non-zero on any mismatch — a proof
    script that reports success on an unchecked delta proves nothing.
  </action>
  <verify>
    <automated>cd ../trustrfq-maker-server &amp;&amp; npm test &amp;&amp; node scripts/live-proof.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `cd ../trustrfq-maker-server &amp;&amp; npm test` exits 0 and its output names at least one
      golden-vector case per vector in `fixtures/rfq-order-vectors.json`
    - `node -e` comparing the two repos' fixture files with `fs.readFileSync` reports them byte-identical
    - `node scripts/live-proof.mjs` exits 0 and prints a 64-hex-character Testnet transaction hash
    - The printed output lists all five balance deltas with measured value equal to expected value
    - Mutating `SPREAD_BPS` in the local environment and re-running `npm test` turns the pricing
      suite red (the pricing assertions have teeth, they are not tautologies)
  </acceptance_criteria>
  <done>A quote fetched live from the deployed maker origin settles on Testnet with every balance
  delta matching the quoted amounts and the maker-paid fee exactly, and the sibling repo's order
  encoding is pinned byte-identical to this repo's.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Confirm the live maker and capture its origin for the CSP change</name>
  <what-built>A `trustrfq-maker-server` repository deployed as a Vercel serverless function at a
  real permanent https origin, holding a persistent Stellar identity, funded with XLM and
  demo-issuer USDC, registered on the live `rfq_registry` with a real stake, and proven by a
  Testnet settlement of one of its own signed quotes.</what-built>
  <how-to-verify>
    1. Open the maker origin in a browser. A GET should return 405 (the server is POST-only), not a
       Vercel 404 — that confirms the function is routed.
    2. From a terminal, POST a `getMakerSideOrder` request (the exact command is printed at the end
       of `scripts/live-proof.mjs`) and confirm a `result.authEntry` comes back in under 3 seconds
       on a WARM call. Then wait several minutes and repeat: this second call is the cold-start
       measurement 03-RESEARCH.md assumption A1 flags as unverified. Note both timings — they are
       recorded in the run record later in this phase.
    3. Confirm the registry entry: `node tools/rfq-registry-live.mjs` is not the right tool here
       (it creates throwaway makers); instead re-run `npm run bootstrap` in the maker repo and read
       its `get_maker` read-back, or invoke `get_maker` with the Stellar CLI.
    4. Open the printed Testnet transaction hash on stellar.expert and confirm the swap executed.
    5. Confirm `maker-keys.json` is NOT in the sibling repo's git index and that `MAKER_SECRET` is
       marked Sensitive in the Vercel project.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the origin serves the function and returns a signed quote
    - Human records the exact origin string (scheme + host, no path) — Plan 03-02 adds it to
      `vercel.json` `connect-src` by hand
    - Human records the warm and cold response timings observed
    - Human confirms the settled transaction on a block explorer
    - Human confirms the secret is Sensitive in Vercel and absent from the sibling repo's git index
  </acceptance_criteria>
  <resume-signal>Type "approved: origin=&lt;https://host&gt; warm=&lt;ms&gt; cold=&lt;ms&gt; tx=&lt;hash&gt;" or describe the issue</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any internet caller → maker serverless function | Unauthenticated, untrusted JSON-RPC input crosses here; the function then signs with the maker's private key |
| maker function → Soroban RPC | The RECORDING-mode simulation the signed entry is derived from |
| maker keypair → Vercel env / local key file | The single most security-critical secret this phase creates |
| bootstrap script → live `rfq_registry` | A staked, on-chain write that cannot be undone without the same key |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `src/config.ts`, Vercel env, `maker-keys.json` | critical | mitigate | `MAKER_SECRET` lives only in a Vercel Sensitive env var scoped to Production and in a gitignored `maker-keys.json`; `.gitignore` carries that filename before the key is ever generated; no log line prints the secret or raw signature bytes; acceptance criteria assert the file is absent from `git ls-files` |
| T-03-02 | Tampering | `src/signing.ts` `priceQuote` | high | mitigate | The maker prices and signs ONLY tokens present in `ALLOWED_TOKENS` (native SAC + the configured USDC SAC, derived at load from `Asset.contractId`); any other token address is refused with `-33601` before any signing path is reached, mirroring `src/core/tokens.ts`'s curated allow-list discipline |
| T-03-03 | Denial of Service | `api/rpc.ts` | medium | accept | D-03 and spec §7.4 defer rate limiting and `-33605` from the v1 reference maker. Accepted for a Testnet demo-scale server; recorded as a known gap in the Phase 3 run record rather than silently omitted. Revisit if the endpoint's uptime starts to matter beyond the demo window |
| T-03-04 | Spoofing | `api/rpc.ts` CORS policy | low | accept | `access-control-allow-origin: *` is deliberate: a maker cannot enumerate taker origins in advance, and the maker's signature over the full economic terms (`require_auth_for_args`, spec §7.1) is the real trust boundary. Matches AirSwap's own open-quote model |
| T-03-05 | Tampering | `scripts/bootstrap.mjs` key handling | high | mitigate | The script refuses to run without an existing `MAKER_SECRET`/`maker-keys.json` unless `--fresh-maker` is passed explicitly, and writes any freshly generated key to disk before the first on-chain write. Prevents the orphaned, permanently staked, un-ejectable registry entry documented in `tools/e2e/rfq-driver.mjs` lines 874-896 |
| T-03-06 | Elevation of Privilege | `api/rpc.ts` input handling | medium | mitigate | The handler validates `jsonrpc`, `method` and every `params` field before `params` reaches pricing or signing; malformed input answers `-32700`/`-32601`/`-33604` instead of throwing an unhandled exception (ASVS V5) |
| T-03-SC | Tampering | npm installs in the new sibling repo | high | mitigate | Package-legitimacy gate satisfied by 03-RESEARCH.md's audit table plus the blocking human checkpoint in Task 1 for the two [SUS] Vercel packages; `@stellar/stellar-sdk` exempt as an existing production dependency of this repo |
</threat_model>

## Flagged Assumptions

Surfaced by the spec-less probe fallback and by 03-RESEARCH.md; neither is resolved, and neither is
silently dropped.

1. **E2E-02 edge-probe row is `unclassified` → `unresolved`.** The deterministic edge probe could
   not classify E2E-02 into any of its data/behavior-shape categories (it is an integration-proof
   requirement, not a data-shape requirement), so it produced no acceptance criteria of its own.
   Every truth in `must_haves` above was derived goal-backward from the ROADMAP success criteria
   instead. Treat the edge coverage for this requirement as unestablished, not as zero-risk.
2. **Cold-start latency versus the taker's fixed 3s fan-out timeout (03-RESEARCH.md A1).** Community
   benchmarks put Vercel Node cold starts near 1s with reported spikes to 2-3s, and there is no
   official SLA. Layered on the maker's own RECORDING-mode simulation this could exceed
   `AbortSignal.timeout(3000)` in `src/data/rfqNetwork.ts` and be silently dropped. Task 4 measures
   it; the measurement, not this assumption, is what the run record will carry.

<verification>
- `cd ../trustrfq-maker-server && npx tsc --noEmit` exits 0
- `cd ../trustrfq-maker-server && npm test` exits 0
- `cd ../trustrfq-maker-server && node scripts/live-proof.mjs` exits 0 and prints a Testnet tx hash
- `get_maker` for the maker address returns the deployed origin's `/api/rpc` URL, both SAC ids, and
  a non-zero stake
- This repository is unchanged: `git status --porcelain` in TrustRFQ shows no new tracked files from
  this plan (D-02 — maker code never lands here)
- `npm test` and `cargo test --manifest-path contracts/Cargo.toml` in this repo remain green and
  `otc_swap`'s wasm hash is untouched (this plan compiles nothing here, so both are regression
  guards only)
</verification>

<success_criteria>
- A real permanent https origin answers Stellar RFQ v1 `getMakerSideOrder` with a genuinely signed
  auth entry, and refuses unknown pairs with `-33601` and unknown methods with `-32601`
- The maker holds a persistent Stellar identity that survives redeploys, funded with XLM and
  demo-issuer USDC
- The maker is registered on the live `rfq_registry` with a real stake and appears on both SAC token
  lists
- One quote fetched from that origin has settled on Testnet with all five balance deltas exact
- The sibling repo's order encoding is pinned byte-identical to this repo's golden vectors
- No maker code and no maker secret exists anywhere in this repository
</success_criteria>

<output>
Create `.planning/phases/03-live-full-rfq-loop/03-01-SUMMARY.md` when done. Record: the sibling repo
path and its first commit hash, the deployed origin, the maker address, the registry transaction
hashes for `set_url` and `add_tokens`, the live-proof transaction hash, and the warm/cold latency
measured at Task 4.
</output>
