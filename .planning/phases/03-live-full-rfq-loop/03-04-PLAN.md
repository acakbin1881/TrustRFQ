---
phase: 03-live-full-rfq-loop
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - docs/evidence/live-rfq-run.md
  - ../trustrfq-maker-server/README.md
autonomous: false
requirements: [E2E-02]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "docs/evidence/live-rfq-run.md records the maker address, the registry entry (url, tokens, stake), both quotes, every transaction hash, the balance deltas per swap, the desk deployment URL, and the maker-paid fee"
    - "The record names the one human-driven Freighter run with its own transaction hash, distinct from the automated run's hashes"
    - "The record carries post-reset re-run instructions naming the maker repo's bootstrap command and this repo's live driver command"
    - "The record states the accepted gaps explicitly: no maker-side rate limiting in v1, a wide-open CORS policy, and platform-env key custody, each with its reason"
    - "The record reports the measured cold and warm maker latency as a measurement, replacing the planning-time assumption"
    - "The maker repository's README documents the post-Testnet-reset recovery sequence end to end, including the warning that regenerating the identity orphans the old registry entry"
    - statement: "A reader who has never seen this project can reproduce the live loop from the record alone after a quarterly Testnet reset"
      verification: backstop
  artifacts:
    - path: "docs/evidence/live-rfq-run.md"
      provides: "The human-readable E2E-02 milestone evidence record (D-11), including the manual Freighter run"
      contains: "Post-reset"
      min_lines: 60
    - path: "../trustrfq-maker-server/README.md"
      provides: "Maker-side post-reset recovery runbook (D-12)"
      contains: "Recovering after a Testnet reset"
  key_links:
    - from: "docs/evidence/live-rfq-run.md"
      to: "docs/evidence/live-rfq-run.json"
      via: "every figure in the prose record is transcribed from the committed machine-readable artifact"
      pattern: "live-rfq-run.json"
    - from: "docs/evidence/live-rfq-run.md"
      to: "../trustrfq-maker-server/scripts/bootstrap.mjs"
      via: "the post-reset instructions name the maker repository's bootstrap command"
      pattern: "bootstrap"
  prohibitions:
    - statement: "MUST NOT present any stubbed component (the local stub maker, a mocked wallet as sole evidence, a hardcoded maker URL, or a patched discovery response) as satisfying the E2E-02 live-loop claim"
      status: unverified
    - statement: "MUST NOT report only a hand-picked successful attempt: every attempt made during a recorded live run, including dropped or timed-out fan-outs, must appear in the run record"
      status: unverified
    - statement: "MUST NOT cite desk interface metrics (click counts, wallet-prompt counts, visitor numbers) as evidence for the milestone claim (POS-D1)"
      status: unverified
---

<objective>
Turn the machine-readable run artifact into the milestone's human-readable evidence record, add the
one human-driven run with a real Freighter wallet, write the post-reset recovery runbook, and hold
the gate that decides when this work reaches production.

Purpose: success criterion 4 is about a future reader. A JSON artifact proves the run happened; it
does not tell someone six months and one Testnet reset later what the run was, which parts were
real, which gaps were knowingly accepted, and what to type to do it again. D-08 also requires the
wallet layer to be real in at least one recorded settle: the automated driver uses a mock Freighter,
so a single human-driven run closes the last place a stub could hide. D-07 closes the phase —
production only follows evidence.

Output: `docs/evidence/live-rfq-run.md`, one Freighter-signed Testnet transaction, a maker-side
recovery runbook, and an explicit go or no-go on the merge.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-live-full-rfq-loop/03-CONTEXT.md
@.planning/phases/03-live-full-rfq-loop/03-RESEARCH.md
@.planning/phases/03-live-full-rfq-loop/03-01-SUMMARY.md
@.planning/phases/03-live-full-rfq-loop/03-02-SUMMARY.md
@.planning/phases/03-live-full-rfq-loop/03-03-SUMMARY.md

@docs/evidence/live-rfq-run.json
</context>

## Artifacts this phase produces

See the same section in `03-01-PLAN.md` for the full phase list. New in this plan:

- New committed file `docs/evidence/live-rfq-run.md`
- `../trustrfq-maker-server/README.md`: a "Recovering after a Testnet reset" section

<tasks>

<task type="auto">
  <name>Task 1: Author the milestone evidence record from the committed artifact</name>
  <files>docs/evidence/live-rfq-run.md</files>
  <read_first>
    - docs/evidence/live-rfq-run.json (the source of every figure in the record — transcribe, never
      re-derive from memory)
    - .planning/phases/03-live-full-rfq-loop/03-01-SUMMARY.md, 03-02-SUMMARY.md and 03-03-SUMMARY.md
      (the deployed origin, the registry transaction hashes, the branch-preview URL, the measured
      latencies, and how many runs were needed)
    - .planning/PROJECT.md (POS-D1: evidence is on-chain proofs and reproducible measurements, never
      interface metrics)
    - docs/superpowers/specs/2026-08-17-rfq-protocol-architecture-design.md sections 5, 6 and 11
      (the protocol claim this record is evidence for, and the repo split it honours)
    - public/otc-config.js (the Testnet-reset checklist these instructions dovetail with)
  </read_first>
  <action>
    Write docs/evidence/live-rfq-run.md as the milestone's evidence record (D-11). Model its level
    of concrete detail on this repository's existing live-proof writeups for rfq_swap (2026-08-18)
    and rfq_registry (2026-08-26): addresses, hashes, exact deltas and a re-run command, with no
    adjective doing work a number should do. There is no template file to copy; that is the model,
    not an import.

    Required sections and content.

    What was proven, one paragraph: a real registered maker, quoting from its own always-on server
    in a separate repository, discovered by the desk through rfq_registry with no hardcoded URL
    anywhere in the flow, settled on Testnet in both directions. List the four ROADMAP success
    criteria and state which artefact proves each.

    The maker: address, the sibling repository name and its first commit hash, the deployed origin,
    the registered endpoint URL, and the fact that the identity is persistent and stays registered
    (D-10) until the next Testnet reset.

    Registry entry: the url, both SAC ids, the staked amount, the rfq_registry contract id, and the
    set_url and add_tokens transaction hashes from plan 03-01.

    Pricing: the configured mid rate and spread (D-04), and the statement that pricing is a fixed
    configured rate rather than a market feed, which is exactly what makes expected and actual
    amounts comparable to the stroop.

    The automated run: the desk deployment URL it drove, the driver command, and one table per
    direction carrying sell amount, quoted receive amount, order id, expiry, transaction hash, and
    the five balance deltas (taker and maker in both assets, plus the fee collector in the maker
    token) each shown as measured against expected. State the maker-paid fee in basis points and in
    stroops, and note that the taker's native delta includes the settlement transaction's own
    fee_charged. Include every fan-out attempt, including any that produced no row, and why.

    Latency: the measured warm and cold response times from the maker origin, stated as
    measurements rather than expectations. If any first attempt was dropped by the desk's 3s
    per-request timeout, say so plainly and name the keep-warm cron as an available mitigation that
    was deliberately not pre-built.

    The manual run: leave a clearly marked, obviously unfilled placeholder section for the
    human-driven Freighter run that Task 2 produces. Task 3 fills it. Do not invent its hash.

    Accepted gaps, each with its reason and disposition: no maker-side rate limiting and no -33605
    in the v1 reference server (D-03 and spec section 7.4 defer it; acceptable at demo scale); a
    wide-open CORS policy (the maker's signature over the economic terms is the trust boundary, not
    CORS); the maker's secret held in a platform environment variable rather than a KMS or HSM
    signer (a v2 hardening item); Testnet only.

    Post-reset re-run instructions, numbered and literal: redeploy and initialise rfq_registry per
    the two-step sequence already documented in public/otc-config.js and paste the new id there;
    point the maker repository at the new registry id and run its bootstrap script, which re-funds,
    re-opens the trustline and inventory, and re-registers with set_url and add_tokens; update the
    maker origin in this repository's vercel.json connect-src if the origin changed; re-run the live
    driver with REAL_MAKER_ADDRESS and BASE_URL set. Name every command exactly.

    One hard constraint on the prose: this record may state protocol facts about signatures (one
    ordinary taker transaction signature per settle, no wallet auth-entry signing on the taker path)
    because that is a claim about the design. It must NOT present click counts, prompt counts or any
    desk usage figure as evidence for the milestone (POS-D1). Evidence here is on-chain: hashes,
    deltas and the registry entry.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const md=fs.readFileSync('docs/evidence/live-rfq-run.md','utf8');const rec=JSON.parse(fs.readFileSync('docs/evidence/live-rfq-run.json','utf8'));const need=[rec.makerAddress,rec.makerUrlOnChain,rec.deskUrl,rec.rfqRegistryId,rec.rfqSwapContractId].concat(rec.directions.map(d=>d.txHash));for(const n of need)if(!md.includes(String(n)))throw new Error('record is missing '+n);for(const h of ['Post-reset','Accepted gaps','Registry entry'])if(!md.includes(h))throw new Error('missing section '+h);if(/\bS[A-Z2-7]{55}\b/.test(md))throw new Error('secret key in record');if(md.split('\n').length<60)throw new Error('record too thin');console.log('record ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/evidence/live-rfq-run.md` contains the maker address, the on-chain maker URL, the desk
      deployment URL, both contract ids and both automated transaction hashes, each transcribed from
      `docs/evidence/live-rfq-run.json`
    - It contains sections headed for the registry entry, the accepted gaps and the post-reset
      re-run instructions
    - It shows, per direction, five balance deltas with measured and expected side by side
    - It states the maker-paid fee in basis points and in stroops
    - It states the measured warm and cold maker latency
    - It contains a clearly marked, unfilled placeholder for the manual Freighter run
    - The node assertion above exits 0, including its secret-key pattern check
    - The file is at least 60 lines
  </acceptance_criteria>
  <done>The milestone's human-readable evidence record exists, every figure traceable to the
  committed JSON artifact, with the manual run still pending.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: The one human-driven run — a real Freighter wallet settles a live quote</name>
  <what-built>An automated live loop settling both directions against the real maker, and an
  evidence record with one section still empty. Everything in that run passed through the mock
  Freighter in tools/e2e/freighter-mock.mjs: a postMessage shim signing with a keypair the driver
  holds. That shim is correct and it is what makes the run reproducible, but it is also the last
  place in the milestone claim where a component is not the real one. D-08 closes it with a single
  human-driven run, following the two-wallet browser precedent from 2026-07-14.</what-built>
  <how-to-verify>
    1. Open the Vercel branch-preview desk URL recorded in `docs/evidence/live-rfq-run.md` in a real
       browser with the Freighter extension installed and set to Testnet.
    2. Use a Testnet account you control that holds XLM. If you pick the XLM to USDC direction the
       desk will prompt to add the USDC trustline during Refresh quotes — that is expected.
    3. Connect the wallet, open the RFQ section, select the pair, enter an amount, click Refresh
       quotes, and confirm a quote row appears from the real maker.
    4. Click Accept quote. Freighter should show exactly one transaction-signing prompt and no
       auth-entry signing prompt. Approve it.
    5. Wait for the settlement panel's View transaction link and copy the transaction hash.
    6. Open the hash on a Testnet block explorer and confirm the swap succeeded and the amounts
       match what the quote row displayed.
    7. Keep DevTools open throughout and note any Content Security Policy or CORS message.
    8. Capture whatever extra evidence is convenient (a screenshot of the settled panel, the wallet
       address used). The transaction hash is the only mandatory item.
  </how-to-verify>
  <acceptance_criteria>
    - A Testnet transaction hash exists that was signed by a real Freighter wallet, distinct from
      both automated-run hashes
    - The human confirms the prompt sequence: one transaction signature for the settle, plus a
      separate changeTrust signature only if a trustline had to be added, and no auth-entry signing
      prompt at any point
    - The human confirms the explorer shows amounts matching the displayed quote
    - The human reports whether the console showed any CSP or CORS message
    - Only a public G-address is shared at this checkpoint; no secret key
  </acceptance_criteria>
  <resume-signal>Type "approved: tx=&lt;hash&gt; wallet=&lt;G...&gt; direction=&lt;xlm-usdc|usdc-xlm&gt; console=&lt;clean|details&gt;" or describe what went wrong</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Fill in the manual run and write the maker-side post-reset runbook</name>
  <precondition>Task 2's checkpoint returned a Freighter-signed Testnet transaction hash and the public wallet address used.</precondition>
  <files>docs/evidence/live-rfq-run.md, ../trustrfq-maker-server/README.md</files>
  <read_first>
    - docs/evidence/live-rfq-run.md (the placeholder section to fill)
    - ../trustrfq-maker-server/README.md (as written in plan 03-01 Task 2)
    - ../trustrfq-maker-server/scripts/bootstrap.mjs (the exact flags, env vars and order of
      operations the runbook must describe, including the fresh-identity guard)
    - public/otc-config.js (the registry redeploy sequence the maker runbook dovetails with)
  </read_first>
  <action>
    Replace the manual-run placeholder in docs/evidence/live-rfq-run.md with the real section: the
    date, the desk deployment URL used, the wallet's public address, the direction, the amount, the
    transaction hash, the observed prompt sequence stated as a protocol property (one ordinary
    transaction signature for the settle, plus a changeTrust signature when a trustline had to be
    opened, and no auth-entry signing prompt on the taker path), and whether the console was clean.
    State plainly that this run used a real Freighter extension while the two automated runs used
    the driver's mock wallet, so a reader knows exactly which evidence covers which layer.

    In the maker repository's README.md, add a section headed "Recovering after a Testnet reset"
    that is a literal runbook rather than a summary: the env vars to update first (RFQ_REGISTRY_ID
    and RFQ_SWAP_CONTRACT_ID, locally and in the Vercel project), the single command to run
    (`npm run bootstrap`), what it does in order (load the existing maker secret or refuse to run,
    Friendbot-fund, open the USDC trustline if absent, pay the maker its USDC inventory from the
    issuer, set_url, add_tokens, then read get_maker back), and the explicit warning that the
    fresh-identity flag generates a NEW keypair and permanently orphans the previous registry entry,
    because no later process can sign an eject for a key that was never kept. Add the companion step
    in the other repository: update the maker origin in TrustRFQ's vercel.json connect-src if the
    origin changed, then re-run the live driver.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const md=fs.readFileSync('docs/evidence/live-rfq-run.md','utf8');const rec=JSON.parse(fs.readFileSync('docs/evidence/live-rfq-run.json','utf8'));const auto=rec.directions.map(d=>d.txHash);const found=(md.match(/[0-9a-f]{64}/g)||[]).filter(h=>!auto.includes(h));if(found.length===0)throw new Error('no manual-run transaction hash distinct from the automated ones');if(/TODO|TBD|placeholder/i.test(md))throw new Error('placeholder still present');const rm=fs.readFileSync('../trustrfq-maker-server/README.md','utf8');if(!/Recovering after a Testnet reset/.test(rm))throw new Error('maker runbook section missing');if(!/fresh/i.test(rm))throw new Error('runbook does not warn about regenerating the identity');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/evidence/live-rfq-run.md` contains a 64-hex transaction hash that is not either of the
      automated run's hashes, alongside the wallet's public address and the direction settled
    - The record no longer contains any placeholder marker
    - The record states which evidence used a real wallet and which used the driver's mock wallet
    - `../trustrfq-maker-server/README.md` contains a "Recovering after a Testnet reset" section
      naming `npm run bootstrap`, the env vars to update first, and the orphaned-entry warning
    - The node assertion above exits 0
  </acceptance_criteria>
  <done>The evidence record is complete: two automated settles with a mock wallet, one human settle
  with a real Freighter wallet, and a literal recovery runbook on both sides of the repo split.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: D-07 gate — does this evidence authorise production?</name>
  <decision>Whether to merge `feat/rfq-milestone` into `main` now that E2E-02 has passed on the
  branch preview. Merging is not a bookkeeping step in this project: `main` auto-deploys to Vercel
  production, so the merge is what puts the new CSP allow-list entry and the RFQ taker path in front
  of anyone who opens the desk.</decision>
  <context>
    D-07 fixed the ordering deliberately: prove on the branch preview, merge only after E2E-02
    passes, and optionally re-verify on production afterwards. The evidence now exists —
    `docs/evidence/live-rfq-run.json` and `docs/evidence/live-rfq-run.md`, two automated settles and
    one Freighter settle, five exact balance deltas per swap, zero CSP violations on a real
    deployment.

    Undo cost if this turns out wrong: a revert commit plus a production redeploy, which is cheap in
    itself but is a coordinated change to a live security posture rather than a local edit. Rated
    costly, not one-way — nothing here requires a data migration and no published contract breaks.

    Two things are deliberately NOT in this gate, and should not be smuggled into it: retiring the
    broadcast/intent layer is Phase 4 (RETIRE-01), and the branch also carries Phase 1 and Phase 2
    work plus the README rewrite, so the merge ships more than this phase.
  </context>
  <options>
    <option id="merge-now">
      <name>Merge to main now</name>
      <pros>Production shows a real registered maker and a working RFQ taker path; the registry
      phone book becomes genuinely useful to anyone who opens the desk, which is the outcome D-10
      exists for. The evidence is fresh and the maker is live.</pros>
      <cons>The merge also ships everything else on the branch. A production redeploy is a change to
      the live CSP posture. The Vercel production URL has not itself been exercised by the live
      driver yet.</cons>
    </option>
    <option id="merge-after-production-check">
      <name>Merge, then immediately re-run the live driver against production</name>
      <pros>Closes D-07's optional post-merge verification in the same sitting; catches any
      production-only difference (a header that differs from preview, a stale cached deployment)
      while the context is fresh.</pros>
      <cons>Costs one more live settle and a few more minutes; the evidence record then needs a
      third transaction hash appended.</cons>
    </option>
    <option id="hold">
      <name>Hold the merge</name>
      <pros>Appropriate if anything in the recorded evidence is unsatisfying — a dropped first
      attempt that suggests the cold-start mitigation should land first, or unrelated branch content
      that is not ready to ship.</pros>
      <cons>The registry keeps pointing at a maker that only branch-preview visitors can exercise;
      the milestone gate stays open.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - The human has read `docs/evidence/live-rfq-run.md` before deciding
    - The decision and its reason are recorded in `03-04-SUMMARY.md`
    - If `merge-after-production-check` is chosen, the third transaction hash is appended to the
      evidence record before the phase is sealed
    - The merge itself is performed by the developer or by `/gsd-ship`; this checkpoint records the
      authorisation, it does not push anything
  </acceptance_criteria>
  <resume-signal>Select: merge-now, merge-after-production-check, or hold</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence record → public repository | A committed record is read as truth later; anything overstated or omitted here becomes a false claim about the protocol |
| branch preview → production | The merge moves a changed CSP posture from a preview nobody visits to the deployment everyone does |
| human wallet → the live maker | A real Freighter signature over a real quote; the one place a person's own key touches this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15 | Information Disclosure | `docs/evidence/live-rfq-run.md` | high | mitigate | The record carries only public facts; Task 1's automated verify greps the committed markdown for the Stellar secret-key pattern and fails on a match. The manual-run checkpoint explicitly asks for a public G-address only |
| T-03-16 | Repudiation | the milestone claim | high | mitigate | The record must state which evidence used a real wallet and which used the driver's mock wallet, must list every fan-out attempt including failures, and must enumerate the accepted gaps with reasons. Task 3's verify fails if any placeholder survives, so an unfilled section cannot ship looking complete |
| T-03-17 | Elevation of Privilege | merge to `main` and the production auto-deploy | medium | mitigate | Gated behind an explicit blocking decision checkpoint (D-07) that requires the evidence to have been read; the checkpoint records authorisation and performs no push itself |
| T-03-18 | Spoofing | the human-driven run | low | accept | The manual run is attested by a person rather than machine-verified, which is inherent to closing the mock-wallet gap. The mitigation is that its transaction hash is on-chain and independently checkable, and that the record names the wallet address used |
</threat_model>

## Flagged Assumptions

1. **E2E-02 edge-probe row is `unclassified` → `unresolved`.** The deterministic edge probe produced
   no classified edges for this requirement, so no probe-derived acceptance criteria exist; every
   truth here was derived goal-backward from the ROADMAP success criteria. Edge coverage for E2E-02
   is unestablished, not zero — which is itself part of why this phase leans so hard on recorded
   on-chain evidence rather than on a test matrix.
2. **Cold-start latency versus the fixed 3s fan-out timeout (03-RESEARCH.md A1).** The record must
   report the measured warm and cold latency and any dropped first attempt. If the measurement shows
   the assumption failing, the keep-warm cron becomes a justified follow-up item and should be
   captured as such rather than quietly fixed inside this phase.
3. **"Reproducible after a quarterly Testnet reset" cannot be verified now.** No reset has occurred,
   so the reproducibility truth is marked `backstop`: the record and the runbook are written for it,
   but the claim is unproven until the next reset actually happens.

<verification>
- `docs/evidence/live-rfq-run.md` exists, is at least 60 lines, and every figure matches
  `docs/evidence/live-rfq-run.json`
- The record contains a manual-run transaction hash distinct from both automated hashes
- The record contains no Stellar secret-key-shaped string and no placeholder marker
- `../trustrfq-maker-server/README.md` contains the post-reset recovery runbook
- `npm test` green (12 files, 167 tests), `npm run build` clean
- `cargo test --manifest-path contracts/Cargo.toml` green and `otc_swap`'s wasm hash unmoved
- The D-07 decision is recorded in `03-04-SUMMARY.md` with its reason
</verification>

<success_criteria>
- The milestone evidence record exists, is complete, and is honest about which layer each piece of
  evidence covers
- One Testnet settlement in the record was signed by a real Freighter wallet
- Post-reset recovery is documented as a literal runbook on both sides of the repo split
- The merge to `main` is explicitly authorised or explicitly held, with the reason recorded
</success_criteria>

<output>
Create `.planning/phases/03-live-full-rfq-loop/03-04-SUMMARY.md` when done. Record: the manual run's
transaction hash and wallet address, the D-07 decision and its reason, any follow-up items the
measured latency produced, and the final state of the maker (still registered, still quoting).
</output>
