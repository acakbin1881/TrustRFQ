# Claude Code — machine-specific local permissions (do not commit)
.claude/settings.local.json

# Claude Code — MCP config contains a live Supabase access token (do not commit)
.mcp.json

.vercel

# Rust / Soroban build artifacts
target/
node_modules

# Vite build output
dist/
.env*

# Claude Design exports (source lives in src/ui/Ticket.tsx + public/styles.css)
*.zip

# Testnet demo funding secrets — NEVER commit
demo-keys.json
