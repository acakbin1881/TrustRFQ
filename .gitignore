# Local development harness: editor/agent configuration, vendored tooling copies and the
# internal planning workspace. Workflow scaffolding, not part of the product.
.claude/
.agents/
.planning/
.gsd/
.superpowers/
skills-lock.json
CLAUDE.md

# Local MCP / tooling config. Holds a live service access token, never commit.
.mcp.json

# Dependencies and build output
node_modules
dist/
dist-rfq/
target/
.vercel
.env*
*.zip
.DS_Store

# Secrets: Testnet demo funding keys and E2E keypairs. NEVER commit.
demo-keys.json
e2e-keys.json

# E2E run artifacts (console logs, reports, failure screenshots)
tools/e2e/out/

# Measurement-tool scratch state. The committed JSONL + summary files are the record.
tools/research/slippage/runs/launchd.log
tools/research/tradesize/runs/.checkpoint-*.json
tools/research/tradesize/runs/*-resolved.json
